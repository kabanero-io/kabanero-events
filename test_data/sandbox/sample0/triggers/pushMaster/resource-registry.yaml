apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  annotations:
  name: docker-image-{{.jobid}}
  namespace: {{.namespace}}
spec:
  params:
  - name: url
    value: {{.registry}}
  type: image
