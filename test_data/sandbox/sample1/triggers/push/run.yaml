apiVersion: tekton.dev/v1alpha1
kind: PipelineRun
metadata:
  name:  {{.build.nameSuffix}}
  namespace: {{.build.namespace}}
spec:
  pipelineRef:
    name: {{.kabanero.collectionID}}-build-push-pipeline
  resources:
  - name: git-source
    resourceRef:
      name: git-push-{{.build.nameSuffix}}
  - name: docker-image
    resourceRef:
      name: docker-push-{{.build.nameSuffix}}
  serviceAccount: {{.build.serviceAccount}}
  timeout: {{.build.timeout}}
